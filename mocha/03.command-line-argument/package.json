{
  "name": "03.command-line-argument",
  "private": true,
  "scripts": {
    "test": "npx mocha --recursive --reporter mochawesome --reporter-options reportDir=.test_output",
    "coverage": "npx nyc npm test"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "mochawesome": "^6.1.1",
    "nyc": "^15.0.1"
  }
}
