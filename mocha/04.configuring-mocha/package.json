{
  "name": "04.configuring-mocha",
  "private": true,
  "scripts": {
    "test": "npx mocha",
    "test-command-inline": "npx mocha --recursive --reporter mochawesome --reporter-options reportDir=.test_output",
    "test-use-mocha-opts": "npx mocha test-use-mocha-opts --no-config --opts=test-use-mocha-opts/mocha.opts"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "mochawesome": "^6.1.1"
  }
}
