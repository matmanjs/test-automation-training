{
  "name": "04.mocha-opts",
  "private": true,
  "scripts": {
    "test2": "npx mocha",
    "test-use-mocha-opts": "npx mocha test-use-mocha-opts",
    "test": "npx mocha --recursive --reporter mochawesome --reporter-options reportDir=.test_output",
    "coverage": "npx nyc npm test"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "mochawesome": "^6.1.1",
    "nyc": "^15.0.1"
  }
}
