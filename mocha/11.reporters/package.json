{
  "name": "11.reporters",
  "private": true,
  "scripts": {
    "test": "mocha",
    "test-reporter-spec": "mocha --reporter spec",
    "test-reporter-dot": "mocha --reporter dot",
    "test-reporter-nyan": "mocha --reporter nyan",
    "test-reporter-tap": "mocha --reporter tap",
    "test-reporter-landing": "mocha --reporter landing",
    "test-reporter-list": "mocha --reporter list",
    "test-reporter-progress": "mocha --reporter progress",
    "test-reporter-json": "mocha --reporter json",
    "test-reporter-json-stream": "mocha --reporter json-stream",
    "test-reporter-min": "mocha --reporter min",
    "test-reporter-doc": "mocha --reporter doc",
    "test-reporter-markdown": "mocha --reporter markdown",
    "test-reporter-xunit": "mocha --reporter xunit",
    "test-reporter-mochawesome": "mocha --reporter mochawesome --reporter-options reportDir=.test_output",
    "test-reporter-junit": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=.test_output/test-result.xml"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "mocha-junit-reporter": "^2.0.0",
    "mochawesome": "^6.1.1"
  }
}
