{
  "name": "11.reporters",
  "private": true,
  "scripts": {
    "test": "npx mocha",
    "test-reporter-spec": "npx mocha --reporter spec",
    "test-reporter-dot": "npx mocha --reporter dot",
    "test-reporter-nyan": "npx mocha --reporter nyan",
    "test-reporter-tap": "npx mocha --reporter tap",
    "test-reporter-landing": "npx mocha --reporter landing",
    "test-reporter-list": "npx mocha --reporter list",
    "test-reporter-progress": "npx mocha --reporter progress",
    "test-reporter-json": "npx mocha --reporter json",
    "test-reporter-json-stream": "npx mocha --reporter json-stream",
    "test-reporter-min": "npx mocha --reporter min",
    "test-reporter-doc": "npx mocha --reporter doc",
    "test-reporter-markdown": "npx mocha --reporter markdown",
    "test-reporter-xunit": "npx mocha --reporter xunit",
    "test-reporter-mochawesome": "npx mocha --reporter mochawesome --reporter-options reportDir=.test_output"
  },
  "author": "helinjiang",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "mochawesome": "^6.1.1"
  }
}
