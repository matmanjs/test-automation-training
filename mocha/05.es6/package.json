{
  "name": "05.es6",
  "private": true,
  "scripts": {
    "test": "npx mocha --require @babel/register --recursive",
    "test-custom-babel-env": "npx cross-env BABEL_ENV=test mocha --require @babel/register --recursive"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.10.2",
    "@babel/register": "^7.10.1",
    "chai": "^4.2.0",
    "cross-env": "^7.0.2",
    "mocha": "^7.2.0"
  }
}
