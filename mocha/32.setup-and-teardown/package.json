{
  "name": "32.setup-and-teardown",
  "private": false,
  "scripts": {
    "test": "mocha --file ./test/setup.js --recursive"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1"
  }
}
