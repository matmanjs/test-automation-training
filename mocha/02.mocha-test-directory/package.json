{
  "name": "02.mocha-test-directory",
  "private": false,
  "scripts": {
    "test": "npx mocha --recursive",
    "test-other-folder": "npx mocha test-other-folder --recursive",
    "test-all": "npm run test && npm run test-other-folder"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1"
  }
}
