{
  "name": "07.async",
  "private": true,
  "scripts": {
    "test": "npx mocha",
    "test-custom-timeout": "npx mocha --timeout=4000"
  },
  "devDependencies": {
    "axios": "^0.19.2",
    "chai": "^4.2.0",
    "mocha": "^7.2.0"
  }
}
